apiVersion: v1
kind: Service
metadata:
  name: {{ include "next-chart.fullname" . }}
  labels:
    {{- include "next-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "next-chart.selectorLabels" . | nindent 4 }}
